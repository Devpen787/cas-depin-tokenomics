% Character encoding and languages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

% Fonts: Time-tested academic choices (Times-like)
\usepackage{times}     % Use Times font for text (simpler, avoids math font errors)
\usepackage{courier}  % Courier for monospace
\usepackage{helvet}   % Helvetica for sans-serif

% Page Layout
\usepackage[a4paper, top=3cm, bottom=3cm, left=3cm, right=3cm]{geometry}

% Line Spacing (1.5 spacing is standard for review/readability)
\usepackage{setspace}
\onehalfspacing

% Prevent widows and orphans while avoiding extreme whitespace side effects.
\widowpenalty=10000
\clubpenalty=10000
\displaywidowpenalty=10000
\raggedbottom
\setlength{\emergencystretch}{3em}

% Math Support
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{mathtools}

% Graphics and Figures
\usepackage{graphicx}
\usepackage{float}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}
\floatplacement{figure}{htbp}
\floatplacement{table}{htbp}
\setcounter{topnumber}{5}
\setcounter{bottomnumber}{5}
\setcounter{totalnumber}{10}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.75}

% Tables
\usepackage{booktabs} % Professional table rules
\usepackage{array}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{tabularx} % Auto-width tables
\usepackage{microtype}

% Bibliography management
\usepackage[backend=biber, style=numeric, sorting=none]{biblatex}
\addbibresource{bibliography.bib}

% Utility
\usepackage{enumerate} % Easy list customization
\usepackage{indentfirst}
\usepackage[pdftex, pdfusetitle]{hyperref}
\usepackage{url}
\usepackage{fancyvrb}
\usepackage{xcolor}
\usepackage{listings}
\lstdefinestyle{rawdraft}{
    basicstyle=\rmfamily\normalsize,
    breaklines=true,
    breakatwhitespace=false,
    columns=fullflexible,
    keepspaces=true,
    frame=none,
    xleftmargin=0em,
    xrightmargin=0em,
    aboveskip=0.8em,
    belowskip=0.8em,
    showstringspaces=false
}


% Hyperref setup (clean, no boxes)
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    filecolor=black,
    urlcolor=black,
    pdfborder={0 0 0},
    plainpages=false,
    pdfpagelabels=true
}

% Fix URL formatting in bibliography
\urlstyle{same} % Use the same font as text for URLs (avoids wide Courier)

% Allow URLs to break on lowercase and uppercase letters to prevent bad spacing
\setcounter{biburllcpenalty}{7000}
\setcounter{biburlucpenalty}{8000}

% Make \paragraph headings block-style (not run-in) for cleaner thesis typography.
\makeatletter
\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
  {0.85\baselineskip}%
  {0.3\baselineskip}%
  {\normalfont\normalsize\bfseries}}

\makeatother

% Provenance rendering switch (default: off for submission builds)
\newif\ifincludeprovenance
\includeprovenancefalse
